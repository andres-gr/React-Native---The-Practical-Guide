mutation AddPlace($placeName: String!, $latitude: Float!, $longitude: Float!, $uri: String!) {
    addPlace(placeName: $placeName, latitude: $latitude, longitude: $longitude, uri: $uri) @client {
        places
    }
}
